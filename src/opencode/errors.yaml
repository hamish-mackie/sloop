version: 1
vendor: opencode
rules:
  - id: opencode.rejection.unexpected-server-error
    class: unknown_rejection
    diagnostic: OpenCode rejected the request with an unclassified server error
    match:
      stream: stderr
      message_signatures:
        - Unexpected server error. Check server logs for details.
